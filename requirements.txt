# --- Orchestration + plugins (local console mode uses your mic/speaker) ---
# Extras: openai (Ollama-compat + OpenAI cloud), silero (VAD),
# turn-detector (endpointing), deepgram (cloud STT+TTS), cerebras (cloud LLM).
livekit-agents[openai,silero,turn-detector,deepgram,cerebras]~=1.6

# --- Local STT ---
mlx-whisper>=0.4.2 ; sys_platform == "darwin" and platform_machine == "arm64"
faster-whisper>=1.1.0

# --- Local TTS ---
kokoro-onnx>=0.4.9
soundfile>=0.12.1

# --- Wake word ---
openwakeword>=0.6.0

# --- Spotify search + misc ---
requests>=2.32.0
numpy>=1.26
python-dotenv>=1.0.1
