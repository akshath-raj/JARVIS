# --- Orchestration + plugins (local console mode uses your mic/speaker) ---
# Extras: openai (Ollama-compat + OpenAI cloud), silero (VAD),
# turn-detector (endpointing), deepgram (cloud STT+TTS), cerebras (cloud LLM).
livekit-agents[openai,silero,turn-detector,deepgram,cerebras]~=1.6

# --- Local STT ---
mlx-whisper>=0.4.2 ; sys_platform == "darwin" and platform_machine == "arm64"
faster-whisper>=1.1.0

# --- Local TTS ---
kokoro-onnx>=0.4.9
soundfile>=0.12.1

# --- LangGraph brain + persistent memory (all pure-python, no torch) ---
langgraph>=1.0
langgraph-supervisor>=0.0.30
langchain-core>=1.0
langchain-ollama>=1.0
livekit-plugins-langchain~=1.6

# --- OpenAI Agents SDK cloud brain (JARVIS_ORCHESTRATOR=openai) ---
# Triage + handoffs to Music/Browser/Screen specialists, plus screen vision.
# Additive — keeps openai 2.x / pydantic 2.x (no LiveKit-stack conflict).
openai-agents>=0.20

# --- Documents (parse + assemble assignment files) ---
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0
nbformat>=5.10

# --- Calendar / reminders (natural-language time parsing) ---
dateparser>=1.2

# --- Spotify search + misc ---
requests>=2.32.0
numpy>=1.26
python-dotenv>=1.0.1

# --- Optional local speaker verification (Siamese owner-voice gate) ---
torch>=2.2
sounddevice>=0.5

# --- Dev / tests ---
pytest>=8.0
pytest-asyncio>=0.24
